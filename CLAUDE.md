# aside-skills 작업 규칙

이 저장소는 **공개**입니다. 그리고 두 곳에서 커밋이 들어옵니다 — 화영님 PC의
Claude Code, 그리고 클라우드 세션(데일리 브리프 루틴 등). 그래서 아래 둘만
지키면 됩니다.

## 1. 작업을 시작하면 먼저 `git pull`

```bash
git pull
```

2026-09-18에 이걸 안 해서 사고가 났습니다. 로컬에 있던 하루 전 파일을 고쳐서
올렸더니, 그 사이 클라우드에서 한 **익명화가 통째로 덮였고** 동료 담당자
실명 5명이 공개 저장소의 최신 파일로 되돌아갔습니다. 파일을 열기 전에
`git pull` 한 번이 그걸 막습니다.

## 2. 예시에는 실제 값을 쓰지 않는다 — `○○` 표기

| 쓰지 않는다 | 쓴다 |
|---|---|
| 실제 학교 이름 | `○○초등학교` |
| 동료 담당자 이름 | `최○○`, `김○○`, `이○○`, `박○○`, `정○○` |
| 경로 속 담당자 이름 | `…\업무파악\박○○\3월` |

이 스킬의 예시에 들어갈 값은 **학교마다 다릅니다.** 그래서 실제 값을 적을
자리가 애초에 없습니다. 특히 **이름과 담당 업무가 한 줄에 같이 있으면**
누가 무슨 일을 맡는지가 그대로 드러납니다 — 본인 정보가 아니라 제3자
정보이므로 공개 저장소에 둘 수 없습니다.

## 3. 커밋 전에 검사

```bash
python scripts/check_anonymized.py
```

통과하면 종료 코드 0, 익명화가 안 된 곳이 있으면 1과 함께 파일·줄번호를
알려 줍니다. 같은 검사가 **push 될 때 GitHub Actions로도 돌아갑니다**
(`.github/workflows/privacy-check.yml`) — 로컬에서 깜빡해도 푸시 직후에 빨간
표시로 잡힙니다.

검사기에는 **실제 이름을 적지 않습니다.** 이름 목록을 넣으면 그 목록 자체가
노출이기 때문에, "○ 가 없는 이름 자리"를 패턴으로 잡습니다. 정상인데 걸리면
`scripts/check_anonymized.py` 의 규칙을 조정하십시오.

## 4. 이미 올라간 민감정보를 지울 때는 PR을 만들지 않는다

**직접 커밋합니다.** PR로 처리하면 지운 내용이 PR 화면의 diff(빨간 줄)로
**영구 보존되고, 공개 저장소에서는 그게 그대로 보입니다.** GitHub에서 머지된
PR은 지울 수 없습니다. 2026-09-17에 이 방식으로 처리했다가 같은 이름이 PR
페이지에 남았습니다.

순서: 직접 커밋 → 필요하면 그 뒤에 이력 정리.

## 함께 쓰는 스킬

- [upmu-pahak](https://github.com/shway81-droid/upmu-pahak) — 이 스킬로 내려받은
  공문 폴더를 「업무 길잡이」 HTML 한 장으로 엮습니다. 같은 규칙이 적용됩니다.
