---
name: "kedufine-document-archive"
description: "K-에듀파인 문서등록대장에서 생산문서를 기안자별로 일괄 다운로드하여 업무별/월별 폴더로 정리한다. 학교 업무분장표 기준으로 문서를 아카이빙하거나, 업무 인수인계용 문서 정리, 특정 기안자의 생산문서 PDF 백업이 필요할 때 사용한다."
---

# K-에듀파인 생산문서 업무별 아카이빙

문서등록대장은 업무별 조회를 지원하지 않고 기안자별 조회만 가능하다. 따라서 기안자로 다운로드한 뒤 폴더명을 업무명으로 바꾸는 우회 방식을 쓴다.

최종 구조: `업무파악 > [업무부서명] > [월별 폴더]`

## 1단계: 업무분장표 확인 및 폴더 생성

업무분장표(PDF/HWP)에서 부서, 계, 담당자를 매칭한다. 한 담당자가 여러 계를 겸하는 경우가 많으므로 폴더명은 부서 단위로 정하되, 같은 부서에 여러 담당자가 있으면 `부서-계` 형태로 구분한다.

예시 매핑:

```text
최연미 (교무기획부 전반)        -> 교무기획부
김덕미 (늘봄학교 돌봄)          -> 늘봄학교-돌봄
김기하 (늘봄학교 방과후)        -> 늘봄학교-방과후
김목   (연구기획부 연구기획 등) -> 연구기획부
박형남 (연구기획부 정보 등)     -> 연구기획부-정보
```

대상 인원을 사용자에게 확인한 뒤, 기안자명으로 임시 폴더와 12개 월별 하위 폴더를 만든다. 월 폴더명은 `3월`~`12월`, 연도가 바뀌면 `2026년 1월` 형식으로 구분한다.

## 2단계: 기안자별 월별 다운로드

문서관리 > 문서함 > 문서등록대장 화면에서 작업한다. iframe 내부이므로 `retrieveDocRegistRegstrList`를 포함한 frame을 잡는다.

### 조회 설정

조회옵션을 펼쳐 상세조회에서 설정한다.

| 항목 | 값 | 비고 |
|---|---|---|
| 기안(접수)자 | 대상자 이름 | `#reportr` |
| 결재유형 | 생산 | 한 번만 설정하면 유지 |
| 소속부서 | 해당 학교 | 기본값 유지 |
| 등록일자 | 월 단위 분할 | 월별 폴더로 나누기 위함 |
| 페이지당 표시 | **100건** | 기본 30건이면 30건 초과 월에서 누락 발생 |

### 필드 ID 주의사항

상세조회 패널이 펼쳐진 상태와 접힌 상태에서 활성 날짜 필드가 다르다. 보이는 쪽에 입력해야 조회에 반영된다.

- 패널 접힘: `#basicReportDe`, `#basicReportDe3`
- 패널 펼침: `#reportDe`, `#reportDe3`

`#reportr`(기안자)는 패널이 접혀도 값이 유지되므로, 기안자만 바꿀 때는 펼쳐서 입력 후 다시 접고 날짜는 basic 필드로 조회한다. 매 조회 전 어느 필드가 보이는지 확인하고 해당 필드에 입력한다.

### 조회 후 확인

`총 N건 / 조회 M건`에서 N과 M이 같아야 한다. 다르면 페이지당 설정이 100이 아니라 뒷페이지가 누락된 상태다.

전체선택 체크박스는 `#chkAll`이다.

### 저장

PC저장 버튼은 `a[onclick*="doCallFileManagerExtra"]`. 클릭하면 별도 네이티브 창(WXSClient)이 뜬다.

파일저장 창 설정:

- 파일구분: 본문 + 첨부파일
- 문서형식: **배포(PDF 형식)**
- 저장형식: (문서번호 (본문)) 문서의제목

이후 폴더 찾아보기 창에서 저장 경로를 지정한다. `scripts/save_month.ps1`이 이 과정을 자동화한다.

```powershell
# 저장 시작 시각 기록 (새로 생성된 파일만 집계하기 위함)
(Get-Date).ToString('o') | Set-Content '<tmp>\download-start.txt'

# PC저장 클릭 후 실행
& '<skill-dir>\scripts\save_month.ps1' `
  -Destination 'C:\Users\User\Desktop\업무파악\김목\3월' `
  -Expected 27 `
  -StartFile '<tmp>\download-start.txt'
```

`-Expected`는 조기 종료 판단용 참고값이며, 미달해도 스크립트는 실패하지 않고 실제 저장 결과를 보고한다.

## 3단계: 폴더명 변경

전체 다운로드 검증 후 기안자명 폴더를 업무부서명으로 바꾼다.

```powershell
Rename-Item -LiteralPath 'C:\Users\User\Desktop\업무파악\김목' -NewName '연구기획부'
```

탐색기가 해당 폴더나 하위 폴더를 열고 있으면 접근 거부가 발생한다. `Shell.Application`으로 창을 닫고 2~3초 대기 후 재시도한다.

## 자동 제외되는 문서

조회 건수와 저장 건수가 다른 것은 정상이다. 다음 두 종류는 시스템이 조용히 건너뛴다.

**비전자문서**: 종이문서로 등록되어 전자 본문이 없다. 목록에서 `비전자문서` 아이콘으로 식별한다. 체크는 되지만 저장 단계에서 제외된다. 근무상황부, 자원봉사활동대장 등 서식/대장류가 해당하며 학년도 말에 몰려 있다.

```js
const nonElec = await frame.locator('img[alt^="비전자문서"]').count();
```

**열람 권한 없는 문서**: 로그인 계정 권한 밖의 문서. 인사, 성과상여금, 범죄경력조회 같은 민감 문서가 해당한다. 교무부장 등 인사 담당자 문서에서 많이 발생한다.

예상 건수는 `총 건수 - 비전자문서 수`로 잡되, 권한 제한분만큼 더 적게 저장될 수 있다. 미달을 오류로 처리하지 말고 그대로 진행한다.

## 검증

각 담당자별로 본문 문서 수와 전체 파일 수를 집계해 보고한다.

```powershell
$files = Get-ChildItem -LiteralPath $path -File
$body = @($files | Where-Object { $_.Name -match '\(본문\)' }).Count
```

누락이 의심되면 목록의 문서번호와 저장된 파일명의 문서번호를 대조한다. 파일명은 `(해제남초등학교-2759 (본문)) 제목.pdf` 형식이다.

## 사용자 작업 간섭

저장 과정에서 네이티브 창이 포커스를 가져가므로 사용자 작업과 충돌한다. 화면 밖으로 창을 옮겨 조작하는 방식은 WXSClient가 종료되거나 응답하지 않아 실패한다. 장시간 작업 전에 사용자에게 알리고, 간섭이 문제되면 사용자가 PC를 쓰지 않는 시간에 진행한다.
